query getBulkOperationStatus {
  currentBulkOperation {
    id
    status # RUNNING / COMPLETED / FAILED
    objectCount
    url # 完了時: NDJSONダウンロードURL
    partialDataUrl # 部分結果（失敗時に一部だけ出ることも）
    errorCode
  }
}
