'use client';

import React from 'react';
import { CreateGuestCustomer } from '@/feature/createGuestCustomer';

const Page: React.FC = () => {
  return <CreateGuestCustomer pageTitle="買取受付フォーム" />;
};

export default Page;
